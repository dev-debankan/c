#include <stdio.h>
#include <conio.h>
int main() //use void instead of int a its made in vs code //// dont foorget to add clrscr()and getch()
{
    int a,b;
    printf("ENTER Two NUMBERS: ");
    scanf("%d%d",&a,&b);
    printf("THE SUM IS : %d",a+b);
   

   
}